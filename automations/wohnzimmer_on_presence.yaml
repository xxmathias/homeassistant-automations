id: wohnzimmer_on_presence
alias: Wohnzimmer on presence
description: Presence-activated living room lighting with manual override respect
triggers:
  - entity_id: binary_sensor.praesenzsensor3_presence
    to: "on"
    id: presence_on
    trigger: state
  - entity_id: binary_sensor.praesenzsensor3_presence
    to: "off"
    id: presence_off
    trigger: state
conditions:
  # Only activate when it's dark enough
  - condition: numeric_state
    entity_id: sensor.sun_solar_elevation
    below: 10
actions:
  - choose:
      # Presence detected - turn on light only if it's off (respect manual changes)
      - conditions:
          - condition: trigger
            id: presence_on
          - condition: state
            entity_id: light.wohnzimmerlampe
            state: "off"
        sequence:
          - choose:
              # Night mode: dimmed warm light
              - conditions:
                  - condition: time
                    after: "23:00:00"
                    before: "07:00:00"
                sequence:
                  - service: light.turn_on
                    target:
                      entity_id: light.wohnzimmerlampe
                    data:
                      brightness_pct: 20
                      kelvin: 2700
                      transition: 2
              # Normal mode: full brightness
              - conditions: []
                sequence:
                  - service: light.turn_on
                    target:
                      entity_id: light.wohnzimmerlampe
                    data:
                      brightness_pct: 100
                      transition: 2
      # Presence lost - start turn-off sequence
      - conditions:
          - condition: trigger
            id: presence_off
        sequence:
          # Wait 10 minutes before dimming
          - delay:
              minutes: 10
          # Check if still no presence
          - condition: state
            entity_id: binary_sensor.praesenzsensor3_presence
            state: "off"
          # Dim the light to 50%
          - service: light.turn_on
            target:
              entity_id: light.wohnzimmerlampe
            data:
              brightness_pct: 50
              transition: 2
          # Wait 1 minute before turning off
          - delay:
              minutes: 1
          # Check if still no presence
          - condition: state
            entity_id: binary_sensor.praesenzsensor3_presence
            state: "off"
          # Turn off light
          - service: light.turn_off
            target:
              entity_id: light.wohnzimmerlampe
            data:
              transition: 2
mode: restart
